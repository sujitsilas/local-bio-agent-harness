# Pinned base package set installed into EVERY Run's uv venv (§5.2).
# The floor every scRNA-seq Run needs. On-demand extras (harmonypy, scvi-tools,
# decoupler, gseapy, celltypist, scrublet) arrive later via the install_package tool.
ipykernel
anndata
scanpy
numpy
pandas
scipy
scikit-misc
igraph
leidenalg
matplotlib
seaborn
